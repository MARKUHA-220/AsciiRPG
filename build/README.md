# Build artifacts

`build/win-x64/AsciiRPG.exe` будет появляться после сборки через `tools/build_windows.bat` (Windows) или эквивалентную команду `dotnet publish`.

В этой среде .NET SDK недоступен, поэтому бинарник не был сгенерирован автоматически.
